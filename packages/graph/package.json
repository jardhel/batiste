{
  "name": "@batiste-aidk/graph",
  "version": "0.1.0",
  "description": "Code-and-knowledge graph for Batiste — symbol/file/doc/fact nodes, deterministic clusters, querier surface consumed by scope (cluster ACLs), audit (semantic annotations), and code MCP tools (find_symbol/validate_code/summarize_codebase). Phase 1 ships the public type contract; implementations land in Phase 2.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "graphology": "^0.25.0",
    "graphology-communities-louvain": "^2.0.2",
    "graphology-types": "^0.24.7"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}
